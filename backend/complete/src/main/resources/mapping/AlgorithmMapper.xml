<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.watermark.mapper.AlgorithmMapper">
    <insert id="addAlgorithm" parameterType="com.watermark.entity.Algorithm">
        INSERT INTO  Watermark.Algorithm (`algorithmId`, `uid`,`name`) VALUES (#{algorithmId},#{uid},#{name});
    </insert>
    <select id="getAlgorithmList" resultType="com.watermark.entity.Algorithm">
        SELECT `algorithmId`,`uid`,`name` FROM Watermark.Algorithm WHERE algorithmId >= (SELECT algorithmId FROM Watermark.Algorithm limit #{start},1) limit #{end};
    </select>

    <select id="getAlgorithmListByUid" resultType="com.watermark.entity.Algorithm">
        SELECT `algorithmId`,`uid`,`name` FROM Watermark.Algorithm WHERE `uid` = #{uid} AND algorithmId >= (SELECT algorithmId FROM Watermark.Algorithm WHERE `uid` = #{uid} limit #{start},1) limit #{end};
    </select>

    <select id="getAlgorithmById" resultType="com.watermark.entity.Algorithm">
        SELECT `algorithmId`,`uid`,`name` FROM Watermark.Algorithm WHERE `algorithmId` = #{algorithmId};
    </select>

    <delete id="delAlgorithm">
        DELETE FROM Watermark.Algorithm WHERE `algorithmId` = #{algorithmId};
    </delete>

    <select id="check" resultType="int">
        SELECT count(*) FROM Watermark.Algorithm WHERE `uid` = #{uid} and `name` = #{name};
    </select>

    <select id="getTot" resultType="int">
        SELECT count(algorithmId) as tot FROM Watermark.Algorithm;
    </select>

    <select id="geUserTot" resultType="int">
        SELECT count(algorithmId) as tot FROM Watermark.Algorithm WHERE uid = #{uid};
    </select>

</mapper>